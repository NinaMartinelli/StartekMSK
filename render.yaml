services:
  - type: web
    name: my-telebot-app
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -w 4 -b :$PORT main:app"
    envVars:
      - key: PORT
        value: 5000
    pythonVersion: 3.8

